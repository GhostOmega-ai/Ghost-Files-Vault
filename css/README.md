# CSS

`files.css` owns the complete Files Vault appearance.

Future visual changes must edit the relevant existing selector or section. Do not append duplicate override blocks or temporary patches. Every CSS release must also include the updated complete root `README.md`.
